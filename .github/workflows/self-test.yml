name: hardware-ci self-test

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/manufacturing.yml"
      - ".github/workflows/self-test.yml"
      - "test/fixture/**"

permissions:
  contents: read

jobs:
  fixture:
    uses: ./.github/workflows/manufacturing.yml
    with:
      board_path: test/fixture/fixture.kicad_pcb
